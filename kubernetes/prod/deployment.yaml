apiVersion: apps/v1
kind: Deployment
metadata:
  name: eatin-ms-statistic-deployment
  labels:
    app: eatin-ms-statistic
spec:
  replicas: 1
  selector:
    matchLabels:
      app: eatin-ms-statistic
  template:
    metadata:
      labels:
        app: eatin-ms-statistic
    spec:
      containers:
        - name: eatin-ms-statistic
          image: eatin.azurecr.io/eatin-ms-statistic
          ports:
            - containerPort: 3000
          env:
            - name: MONGODB_USERNAME
              valueFrom:
                secretKeyRef:
                  name: db-secret
                  key: mongo-root-username
            - name: MONGODB_PASSWORD
              valueFrom:
                secretKeyRef:
                  name: db-secret
                  key: mongo-root-password
---
apiVersion: v1
kind: Service
metadata:
  name: eatin-ms-statistic-service
spec:
  selector:
    app: eatin-ms-statistic
  ports:
    - protocol: TCP
      port: 3000
      targetPort: 3000
